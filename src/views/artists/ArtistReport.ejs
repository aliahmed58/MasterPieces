<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <link rel="stylesheet" href="/css/bootstrap.min.css" />
    <link rel="stylesheet" href="/public/report.css" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Artist Report</title>
</head>

<body>
    <%- include('../partials/navbar') %>

        <div class="container-fluid text-center">
            <h3 class="text-center mt-3">Artist Report</h1>
            <a  href="/artists/edit?artistID=<%= artists[0][0] %> " class="btn btn-primary text-center">Edit Profile Info</a>
        </div>

        <hr>
        <div class="container-fluid">
            <div class="container subcontainer card shadow rounded">
                <div class="row info text-center">
                    <div class="col-sm">
                        <span class="align-middle">Artist no.: <%= artists[0][0]  %>  </span>
                    </div>
                    <div class="col-sm">
                        <span class="align-middle">Year of birth: <%= String(artists[0][4]).slice(11, 15) %> </span>
                    </div>
                    <div class="col-sm">
                        <span class="align-middle">Age: <%= artists[0][7] %> </span>
                    </div>
                </div>
                <div class="row info text-center">
                    <div class="col-sm ">
                        <span class="align-middle">Artist Name: <%= artists[0][1] %> <%= artists[0][2] %>   </span>
                    </div>
                    <div class="col-sm">
                        <% if (artists[0][5] !== null) { %>
                            <span class="align-middle">Year of death: <%= String(artists[0][5]).slice(11, 15) %> </span>
                        <% } else { %>
                            <span class="align-middle">Year of death: (alive)</span>
                        <% } %>
                    </div>
                    <div class="col-sm">
                        <span class="align-middle">Country of birth: <%= artists[0][3] %> </span>
                    </div>
                </div>
            </div>
        </div>

        <hr>

        <div class="contianer table-container">
            <table class="table text-center table-bordered table-responsive">
                <thead>
                    <tr>
                        <th scope="col">Painting no.</th>
                        <th scope="col">Painting Title</th>
                        <th scope="col">Painting Theme</th>
                        <th scope="col">Rental Price (monthly)</th>
                        <th scope="col">Owner no.</th>
                        <th scope="col">Owner Name</th>
                        <th scope="col">Owner Tel</th>
                    </tr>
                </thead>

                <tbody>
                    <!-- use for loop to fill the table -->
                    <% for( let i=0; i < data.length; i++ ) { %>


                        <tr>
                            <td scope="row">
                                <%= data[i][7] %>
                            </td>
                            <td><%= data[i][8] %> </td>
                            <td><%= data[i][9] %></td>
                            <td>
                                <%= data[i][10] %>$
                            </td>
                            <td>
                                <%= data[i][11] %>
                            </td>
                            <td> <%= data[i][12] %> <%= data[i][13] %> </td>
                            <td><%= data[i][14] %></td>
                        </tr>

                        <% } %>

                </tbody>

            </table>
        </div>


</body>

</html>