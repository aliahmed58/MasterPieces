<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <link rel="stylesheet" href="/css/bootstrap.min.css" />
    <link rel="stylesheet" href="/public/liststyles.css" />
    <link rel="stylesheet" href="/public/global.css" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Artists</title>
</head>

<body>
    <%- include('../partials/navbar') %>

    <% if (data.length === 0) { %>
        <div class="container text-center" style="margin-top: 40px;">
            <h3 >No artist records found.</h3>
            <p ><a href="/artists/new" class="link-info">Insert</a> a new artist or visit the <a href="/help" class="link-info">help</a> page</p>
        </div>
    <% } %>

        <!-- each row shall display 4 cards -->
        <div class="row">
                <% for( let row=0; row < data.length; row++ ) { %>
                <div class="col-sm-4">
                    <div class="card shadow rounded">
                        <div class="card-header">
                            Artist no: <%= data[row][0] %>
                        </div>
                        <div class="card-body">
                            <h5 class="card-title">Artist Name: <%= data[row][1] %>
                            </h5>
                            <a href="/artists/report/?artistID=<%=data[row][0] %>" class="btn">View Report</a>
                            <a href="/artists/delete/?artistID=<%= data[row][0] %>" class="btn">Delete</a>
                        </div>
                    </div>
                </div>
                <% } %>
            </div>


</body>

</html>