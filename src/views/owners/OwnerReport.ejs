<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <link rel="stylesheet" href="/css/bootstrap.min.css" />
    <link rel="stylesheet" href="/public/report.css" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Return to Owner Report</title>
</head>

<body>
    <%- include('../partials/navbar') %>

        <div class="container-fluid text-center">
            <h3 class="text-center mt-3">Return to Owner Report</h1>
            <a  href="/owners/edit?ownerID=<%= ownerInfo[0] %> " class="btn btn-primary text-center">Edit Profile Info</a>
        </div>

        <hr>
        <div class="container-fluid">
            <div class="container subcontainer card shadow rounded">
                <div class="row info text-center">
                    <div class="col-sm">
                        <span class="align-middle">Owner number: <%= ownerInfo[0] %>  </span>
                    </div>
                    <div class="col-sm">
                        <span class="align-middle">Owner Name: <%= ownerInfo[1] %> <%= ownerInfo[2] %>  </span>
                    </div>
                </div>
                <div class="row info text-center">
                    <div class="col-sm">
                        <span class="align-middle">Owner Address: <%= ownerInfo[5] %>, <%= ownerInfo[3] %>, <%= ownerInfo[4] %>   </span>
                    </div>
                </div>
            </div>
        </div>

        <hr>

        <div class="contianer table-container">
            <table class="table text-center table-bordered table-responsive">
                <thead>
                    <tr>
                        <th scope="col">Painting no.</th>
                        <th scope="col">Painting Title</th>
                        <th scope="col">Status</th>
                        <th scope="col">Return Date</th>
                        <th scope="col">Resubmit</th>
                    </tr>
                </thead>

                <tbody>
                    <!-- use for loop to fill the table -->
                    <% for( let i=0; i < data.length; i++ ) { %>
                        <tr>
                            <td scope="row"><%= data[i][0] %> </td>
                            <td><%= data[i][1] %> </td>
                            <td><%= data[i][2] %> </td>
                            <td>
                                <% if (data[i][2] !== 'returned') { %>
                                    -
                                <% } else { %>
                                    <%= String(data[i][3]).slice(0, 15) %> 
                                <% } %> 
                            </td>
                            <td>
                                <% if (data[i][2] === 'returned') { %>
                                    <a href="/owners/report/resubmit?pid=<%= data[i][0] %>&ownerID=<%= ownerInfo[0] %>" class="btn">Resubmit</a>
                                <% } else { %>
                                    <a class="btn disabled">Resubmit</a>
                                <% } %> 
                            </td>
                        </tr>
                        
                        <% } %>

                </tbody>

            </table>
        </div>


</body>

</html>