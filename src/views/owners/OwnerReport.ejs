<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <link rel="stylesheet" href="/css/bootstrap.min.css" />
    <link rel="stylesheet" href="/public/report.css" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Return to Owner Report</title>
</head>

<body>
    <%- include('../partials/navbar') %>

        <div class="container-fluid">
            <h3 class="text-center mt-3">Return to Owner Report</h1>
        </div>

        <hr>
        <div class="container-fluid">
            <div class="container subcontainer card shadow rounded">
                <div class="row info text-center">
                    <div class="col-sm">
                        <span class="align-middle">Owner number: <%= data.info.id %>  </span>
                    </div>
                    <div class="col-sm">
                        <span class="align-middle">Owner Name: <%= data.info.name %> </span>
                    </div>
                </div>
                <div class="row info text-center">
                    <div class="col-sm">
                        <span class="align-middle">Customer Address: <%= data.info.address %> </span>
                    </div>
                </div>
            </div>
        </div>

        <hr>

        <div class="contianer table-container">
            <table class="table text-center table-bordered table-hover table-responsive">
                <thead>
                    <tr>
                        <th scope="col">Painting no.</th>
                        <th scope="col">Painting Title</th>
                        <th scope="col">Return Date</th>
                    </tr>
                </thead>

                <tbody>
                    <!-- use for loop to fill the table -->
                    <% for( let i=0; i < data.report_info.length; i++ ) { %>
                        <tr>
                            <th scope="row"><%= data.report_info[i].painting_no %> </th>
                            <td><%= data.report_info[i].painting_title %> </td>
                            <td><%= data.report_info[i].return_date %> </td>
                        </tr>

                        <% } %>

                </tbody>

            </table>
        </div>


</body>

</html>